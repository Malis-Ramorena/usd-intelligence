{
  "devDependencies": {
    "gh-pages": "^6.3.0"
  },
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "postinstall": "npm --prefix frontend install && npm --prefix server install"
  }
}
